import { Options } from 'contain-by-screen';
export default class ScrollableContainByScreen {
    private _manualRepositions;
    private _stopper;
    constructor(element: HTMLElement, anchorPoint: HTMLElement, options: Options);
    reposition(): void;
    destroy(): void;
}
//# sourceMappingURL=ScrollableContainByScreen.d.ts.map