import type { PiOpts, PlatformImplementation } from '../../platform-implementation-js/platform-implementation';
export declare class PlatformImplementationLoader {
    /**
     * This must be overridden by the entrypoint to the InboxSDK.
     * This is done so the npm (non-remote) build doesn't contain code for dynamically
     * loading the remote build, which may set off the Chrome Web Store review process
     * scanning for that.
     */
    static loadScript: () => Promise<void>;
    static load(appId: string, opts: Partial<PiOpts>): Promise<PlatformImplementation>;
    static preload(): Promise<void>;
}
//# sourceMappingURL=platform-implementation-loader.d.ts.map